# Sprint 2.1 — LLM Gateway (AI 基础设施) 实现计划

> **For agentic workers:** Use superpowers:subagent-driven-development to implement task-by-task.

**Goal:** 建立 AI 基础设施层 — LLMProvider 抽象、Claude 实现、工厂模式、统一调用入口

**Architecture:** 抽象基类 + 具体实现 + 工厂模式。业务层只依赖 `LLMProvider` 接口，不依赖 Claude SDK。

**Tech Stack:** anthropic SDK, Pydantic v2, python-decouple (已有 settings)

---

## 文件结构

```
backend/app/ai/
├── __init__.py
├── providers/
│   ├── __init__.py
│   ├── base.py          # LLMProvider 抽象基类
│   ├── claude.py        # ClaudeProvider
│   └── factory.py       # get_llm_provider() 工厂函数
├── prompts/
│   ├── __init__.py
│   ├── memory_extraction.py
│   └── persona_summary.py
├── schemas/
│   ├── __init__.py
│   ├── extraction.py
│   └── chat.py
└── services/
    ├── __init__.py
    └── llm_service.py   # 业务层统一入口
```

---

### Task 2.1.1: LLM Provider 抽象基类

**Files:**
- Create: `backend/app/ai/__init__.py`
- Create: `backend/app/ai/providers/__init__.py`
- Create: `backend/app/ai/providers/base.py`

```python
# backend/app/ai/providers/base.py
from abc import ABC, abstractmethod

class LLMProvider(ABC):
    """LLM Provider 抽象基类，所有 LLM 实现必须继承此类"""

    @abstractmethod
    async def chat(self, messages: list[dict], **kwargs) -> str:
        """发送对话消息，返回文本回复"""
        ...

    @abstractmethod
    async def chat_stream(self, messages: list[dict], **kwargs):
        """流式对话，yield 文本片段"""
        ...

    @abstractmethod
    async def extract(self, text: str, extraction_schema: dict) -> list[dict]:
        """从自然语言文本中提取结构化信息"""
        ...
```

---

### Task 2.1.2: ClaudeProvider 实现

**Files:**
- Create: `backend/app/ai/providers/claude.py`
- Modify: `backend/requirements.txt` (add anthropic)
- Modify: `backend/pyproject.toml` (add anthropic)

```python
# backend/app/ai/providers/claude.py
from anthropic import AsyncAnthropic
from app.ai.providers.base import LLMProvider
from app.config import settings

class ClaudeProvider(LLMProvider):
    def __init__(self, api_key: str | None = None, model: str = "claude-sonnet-4-6"):
        self.client = AsyncAnthropic(api_key=api_key or settings.ANTHROPIC_API_KEY)
        self.model = model

    async def chat(self, messages: list[dict], max_tokens: int = 1024, **kwargs) -> str:
        # messages: [{"role": "user", "content": "..."}, ...]
        system = None
        chat_messages = []
        for m in messages:
            if m["role"] == "system":
                system = m["content"]
            else:
                chat_messages.append(m)

        kwargs_call = dict(model=self.model, max_tokens=max_tokens, messages=chat_messages, **kwargs)
        if system:
            kwargs_call["system"] = system

        response = await self.client.messages.create(**kwargs_call)
        return response.content[0].text

    async def chat_stream(self, messages: list[dict], max_tokens: int = 1024, **kwargs):
        system = None
        chat_messages = []
        for m in messages:
            if m["role"] == "system":
                system = m["content"]
            else:
                chat_messages.append(m)

        kwargs_call = dict(model=self.model, max_tokens=max_tokens, messages=chat_messages, **kwargs)
        if system:
            kwargs_call["system"] = system

        async with self.client.messages.stream(**kwargs_call) as stream:
            async for text in stream.text_stream:
                yield text

    async def extract(self, text: str, extraction_schema: dict) -> list[dict]:
        """从文本中提取结构化信息"""
        import json
        system_prompt = f"""你是一个信息提取助手。从用户输入的文本中提取结构化信息。

输出格式要求（严格遵守 JSON Schema）：
{json.dumps(extraction_schema, ensure_ascii=False, indent=2)}

只返回 JSON 数组，不要包含其他文字。如果没有可提取的信息，返回空数组 []。"""

        response = await self.chat([
            {"role": "system", "content": system_prompt},
            {"role": "user", "content": text},
        ], max_tokens=2048, temperature=0.1)

        # Parse JSON from response
        text_response = response.strip()
        if text_response.startswith("```"):
            text_response = text_response.split("\n", 1)[1].rsplit("\n```", 1)[0]
            if text_response.startswith("json"):
                text_response = text_response[4:].strip()
        return json.loads(text_response)
```

Note: settings.py needs `ANTHROPIC_API_KEY` added. And the user's proxy is at `http://127.0.0.1:65532`. The anthropic SDK needs proxy configuration.

### Update config.py

```python
# Add to Settings class in backend/app/config.py
ANTHROPIC_API_KEY: str = ""
ANTHROPIC_BASE_URL: str = ""  # Optional: for proxies/alternative endpoints
```

### Update pyproject.toml

Add `"anthropic>=0.39.0"` to dependencies.

---

### Task 2.1.3: LLM Provider 工厂

**Files:**
- Create: `backend/app/ai/providers/factory.py`

```python
# backend/app/ai/providers/factory.py
from app.ai.providers.base import LLMProvider
from app.config import settings

_provider: LLMProvider | None = None

def get_llm_provider() -> LLMProvider:
    global _provider
    if _provider is None:
        provider_type = getattr(settings, "LLM_PROVIDER", "claude")
        if provider_type == "claude":
            from app.ai.providers.claude import ClaudeProvider
            _provider = ClaudeProvider()
        else:
            raise ValueError(f"Unknown LLM provider: {provider_type}")
    return _provider

def set_llm_provider(provider: LLMProvider):
    global _provider
    _provider = provider
```

---

### Task 2.1.4: AI Schemas + Prompts

**Files:**
- Create: `backend/app/ai/schemas/__init__.py`
- Create: `backend/app/ai/schemas/extraction.py`
- Create: `backend/app/ai/schemas/chat.py`
- Create: `backend/app/ai/prompts/__init__.py`
- Create: `backend/app/ai/prompts/memory_extraction.py`
- Create: `backend/app/ai/prompts/persona_summary.py`

```python
# schemas/extraction.py
from pydantic import BaseModel

class MemoryExtractionItem(BaseModel):
    persona_name: str           # 人物名称
    category: str               # food/hobby/style/personality/relationship/dream/dislike/other
    content: str                # 记忆内容
    keywords: str = ""          # 逗号分隔的关键词
    importance: int = 5         # 1-10

class MemoryExtractionResult(BaseModel):
    items: list[MemoryExtractionItem]
```

```python
# schemas/chat.py
from pydantic import BaseModel

class ChatRequest(BaseModel):
    message: str
    persona_id: str | None = None  # 当前上下文人物
    session_id: str | None = None

class ChatResponse(BaseModel):
    reply: str
    intent: str | None = None
```

```python
# prompts/memory_extraction.py
MEMORY_EXTRACTION_SYSTEM_PROMPT = """你是一个关系记忆提取助手。从用户输入的文本中，提取关于特定人物的结构化记忆信息。

提取规则：
1. 每条记忆必须明确归属于某个已知人物（如：妈妈、爸爸、小雨等）
2. 分类标准：
   - food: 饮食喜好（喜欢/不喜欢吃的食物）
   - hobby: 兴趣爱好（运动、娱乐、收藏等）
   - style: 审美偏好（颜色、品牌、风格等）
   - personality: 性格特点（内向、开朗、细心等）
   - relationship: 关系相关（家庭关系、朋友关系等）
   - dream: 梦想愿望（想去的地方、想做的事等）
   - dislike: 讨厌/禁忌（讨厌的东西、过敏等）
   - other: 其他值得记录的信息
3. 每条记忆提取 2-5 个关键词
4. 重要度 1-10：基本喜好=5，生日/纪念日=10，随口一提=3
5. 如果没有可提取的新记忆，返回空数组

输出格式：JSON 数组
"""

# prompts/persona_summary.py
PERSONA_SUMMARY_SYSTEM_PROMPT = """你是一个人物画像分析助手。根据提供的记忆数据，生成人物摘要。

基于以下信息生成结构化的人物画像：
- 记忆列表（category + content）
- 事件列表
- 观察记录

输出格式：
{
  "personality": "性格描述（50字以内）",
  "interests": "兴趣标签（逗号分隔）",
  "summary": "综合摘要（100字以内）",
  "gift_suggestions": "送礼建议（3个，逗号分隔）"
}
"""
```

---

### Task 2.1.5: LLM Service 统一入口

**Files:**
- Create: `backend/app/ai/services/__init__.py`
- Create: `backend/app/ai/services/llm_service.py`

```python
# backend/app/ai/services/llm_service.py
from app.ai.providers.factory import get_llm_provider
from app.ai.schemas.extraction import MemoryExtractionResult, MemoryExtractionItem
from app.ai.prompts.memory_extraction import MEMORY_EXTRACTION_SYSTEM_PROMPT
from app.ai.prompts.persona_summary import PERSONA_SUMMARY_SYSTEM_PROMPT
import json

class LLMService:
    def __init__(self):
        self.provider = get_llm_provider()

    async def chat(self, message: str, context: list[dict] | None = None) -> str:
        messages = context or []
        messages.append({"role": "user", "content": message})
        return await self.provider.chat(messages)

    async def chat_stream(self, message: str, context: list[dict] | None = None):
        messages = context or []
        messages.append({"role": "user", "content": message})
        async for chunk in self.provider.chat_stream(messages):
            yield chunk

    async def extract_memories(self, text: str) -> list[MemoryExtractionItem]:
        extraction_schema = {
            "type": "array",
            "items": {
                "type": "object",
                "properties": {
                    "persona_name": {"type": "string", "description": "人物名称"},
                    "category": {"type": "string", "enum": ["food", "hobby", "style", "personality", "relationship", "dream", "dislike", "other"]},
                    "content": {"type": "string", "description": "记忆内容"},
                    "keywords": {"type": "string", "description": "逗号分隔的关键词"},
                    "importance": {"type": "integer", "minimum": 1, "maximum": 10},
                },
                "required": ["persona_name", "category", "content"],
            },
        }

        system_context = [{"role": "system", "content": MEMORY_EXTRACTION_SYSTEM_PROMPT}]
        result_text = await self.provider.chat(
            system_context + [{"role": "user", "content": text}],
            max_tokens=2048,
            temperature=0.1,
        )

        # Parse JSON from response
        cleaned = result_text.strip()
        if cleaned.startswith("```"):
            cleaned = cleaned.split("\n", 1)[1].rsplit("\n```", 1)[0]
            if cleaned.startswith("json"):
                cleaned = cleaned[4:].strip()

        items_data = json.loads(cleaned)
        return [MemoryExtractionItem(**item) for item in items_data]

    async def generate_persona_summary(self, persona_name: str, memories: list[dict], events: list[dict]) -> dict:
        context = f"""人物：{persona_name}

记忆列表：
{json.dumps(memories, ensure_ascii=False, indent=2)}

事件列表：
{json.dumps(events, ensure_ascii=False, indent=2)}"""

        result = await self.provider.chat([
            {"role": "system", "content": PERSONA_SUMMARY_SYSTEM_PROMPT},
            {"role": "user", "content": context},
        ], max_tokens=1024, temperature=0.3)

        cleaned = result.strip()
        if cleaned.startswith("```"):
            cleaned = cleaned.split("\n", 1)[1].rsplit("\n```", 1)[0]
        return json.loads(cleaned)
```

---

### Task 2.1.6: 集成验证

1. 验证 import 链路：`from app.ai.services.llm_service import LLMService` 无报错
2. 使用 Claude API 发送测试消息（需要有效的 ANTHROPIC_API_KEY）
3. 验证记忆提取：输入 "妈妈喜欢百合花和喝绿茶"，得到结构化输出
4. Commit

---

## Commit 顺序

```bash
git commit -m "feat(ai): add LLMProvider abstract base class"
git commit -m "feat(ai): add ClaudeProvider with chat/extract methods"
git commit -m "feat(ai): add LLM provider factory"
git commit -m "feat(ai): add AI schemas and prompt templates"
git commit -m "feat(ai): add LLMService unified entry point"
```
